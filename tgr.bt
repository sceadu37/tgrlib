//------------------------------------------------
//--- 010 Editor v14.0 Binary Template
//
//      File: tgr.bt
//   Authors: Sceadu
//   Version: 1.1
//   Purpose: Parse animation assets from Kohan: Ahriman's Gift
//  Category: Game
// File Mask: *.TGR
//  ID Bytes: 46 4F 52 4D [+4] 54 47 41 52
//   History:
//   1.1   2026-08-25 Sceadu: Improved abstraction & misc. code cleanup
//   1.0   2026-03-06 Sceadu: Initial version.
//
//------------------------------------------------
//
// This Template will work with most TGRs, with or without a palette
// For more information, see https://github.com/tim-de/tgrlib

LittleEndian();
BitfieldDisablePadding();
ThemeAutoScaleColors(false);

typedef struct {
    uint16 ulx;
    uint16 uly;
    uint16 lrx;
    uint16 lry;   
    uint32 offset;
} FRAME_SIZE;

typedef struct {
    uint16 start_frame;
    uint16 frame_count;
    uint16 animation_count; // # of camera angles for the animation
} ANIMATION;

typedef uint32 CHUNK_LENGTH <fgcolor=0x3C8E38>;

typedef uchar IFF_NAME[4] <bgcolor=0x3C8E38,
                           read=Str("%s", this),
                           write=( this = value )>;

// Represents an 'immediate color' pixel (16 bits representing color)
typedef struct {
    LittleEndian();
    ubyte blue : 5;
    ubyte green : 6;
    ubyte red : 5;
} IMM_PIXEL <optimize=false,bgcolor=PixelColor,comment=ImmColorComment>;

// Represents an 'indexed color' pixel (8 bits representing palette index)   
typedef struct {
    ubyte index;
} IND_PIXEL <optimize=false, bgcolor=IndPixelColor, comment=IndColorComment>;

// Represents two 'player color' pixels, packed into one byte. 
// Perform (pixelN << 1) | 1 to calculate the actual index value
typedef struct {
    BigEndian();
    ubyte pixel1 : 4;
    ubyte pixel2 : 4;
    LittleEndian();
} PLYR_PIXEL <optimize=false,bgcolor=0x0000FF>;

int64 PixelColor( IMM_PIXEL &p ) {
    local uint64 r8 = (double)p.red / 31.0 * 255.0;
    local uint64 g8 = (double)p.green / 63.0 * 255.0;
    local uint64 b8 = (double)p.blue / 31.0 * 255.0;
    return (b8 << 16 | g8 << 8 | r8);
}

string ImmColorComment ( IMM_PIXEL &p ) {
    local double r8 = (double)p.red / 31.0 * 255.0;
    if ( r8 > (Floor(r8) + 0.5) ) {
        r8 = Ceil(r8);
    } else {
        r8 = Floor(r8);
    }
    local double g8 = (double)p.green / 63.0 * 255.0;
    if ( g8 > (Floor(g8) + 0.5) ) {
        g8 = Ceil(g8);
    } else {
        g8 = Floor(g8);
    }
    local double b8 = (double)p.blue / 31.0 * 255.0;
    if ( b8 > (Floor(b8) + 0.5) ) {
        b8 = Ceil(b8);
    } else {
        b8 = Floor(b8);
    }
    return Str("r:%d g:%d b:%d", r8, g8, b8);
}

int64 IndPixelColor( IND_PIXEL &p ) {
    local uint64 r8 = (double)PALT.pixels[p.index].red / 31.0 * 255.0;
    local uint64 g8 = (double)PALT.pixels[p.index].green / 63.0 * 255.0;
    local uint64 b8 = (double)PALT.pixels[p.index].blue / 31.0 * 255.0;
    return (b8 << 16 | g8 << 8 | r8);
}

string IndColorComment ( IND_PIXEL &p ) {
    local double r8 = (double)PALT.pixels[p.index].red / 31.0 * 255.0;
    if ( r8 > (Floor(r8) + 0.5) ) {
        r8 = Ceil(r8);
    } else {
        r8 = Floor(r8);
    }
    local double g8 = (double)PALT.pixels[p.index].green / 63.0 * 255.0;
    if ( g8 > (Floor(g8) + 0.5) ) {
        g8 = Ceil(g8);
    } else {
        g8 = Floor(g8);
    }
    local double b8 = (double)PALT.pixels[p.index].blue / 31.0 * 255.0;
    if ( b8 > (Floor(b8) + 0.5) ) {
        b8 = Ceil(b8);
    } else {
        b8 = Floor(b8);
    }
    return Str("r:%d g:%d b:%d", r8, g8, b8);
}

typedef struct {
    BigEndian();
    local uchar run_header = ReadUByte();
    if (run_header >= 0xF8 && run_header <= 0xFF) {
        ubyte unknown_data[2];
    } else {
        switch(run_header >> 5) {
            case 0b000:     // Transparent spacing pixels
                ubyte run_type : 3 <comment="Transparent Padding">;
                ubyte ct_transparent_pixels : 5;
                break;
            case 0b001:     // Run length encoded run of pixels
                ubyte run_type : 3 <comment="RLE single-color run">;
                ubyte run_length : 5;
                if (HEDR.bit_depth == 8) {
                    IND_PIXEL ind_pixel;
                } else {
                    IMM_PIXEL pixel;
                }
                break;
            case 0b010:     // Unencoded run of pixels
                ubyte run_type : 3 <comment="RLE multi-color run">;
                ubyte run_length : 5;
                if (HEDR.bit_depth == 8) {
                    IND_PIXEL ind_pixels[run_length];
                } else {
                    IMM_PIXEL pixels[run_length];
                }
                break;
            case 0b011:     // Run length encoded run of translucent
                ubyte run_type : 3 <comment="RLE single-color run with alpha">;
                ubyte run_length : 5;
                
                if (run_length >= 24) {
                    
                } else {
                    ubyte alpha;
                    if (HEDR.bit_depth == 8) {
                        IND_PIXEL ind_pixel;
                    } else {
                        IMM_PIXEL pixel;
                    }
                }
                break;
            case 0b100:     // Sets the brightness of a single translucent pixel
                ubyte run_type : 3 <comment="Single pixel with alpha">;
                ubyte alpha : 5 <comment=Str("24bit alpha: %i", this*255/31)>;
                if (HEDR.bit_depth == 8) {
                    IND_PIXEL ind_pixel;
                } else {
                    IMM_PIXEL pixel;
                }
                break;
            case 0b101:     // Shadow pixels in sprites
                ubyte run_type : 3 <comment="Shadow pixels">;
                ubyte ct_shadow_pixels : 5;
                break;
            case 0b110:     // One Player Color pixel
                ubyte run_type : 3 <comment="Single player-colored pixel", bgcolor=cRed>;
                ubyte player_color : 5;
                break;
            case 0b111:     // Run Length encoded run of player color pixels
                local ubyte flag = ReadByte();
                if ((flag & 0b00011111) > 0b11011) {
                    ubyte run_type : 3 <comment="Translucent player-pixel">;
                    ubyte fixed_bits : 3;
                    ubyte ix_l : 2;
                    ubyte ix_h : 3 <comment="Use (ix_h<<2)|ix_l for correct index">;
                    ubyte alpha : 5 <read=Str( this / 31 * 255 )>;
                } else {
                    ubyte run_type : 3 <comment="RLE player-pixel run">;
                    ubyte run_length : 5;                
                    PLYR_PIXEL player_pixels[(run_length + 1) >> 1];  // Since each byte holds 2 pixels, divide taking the ceiling 
                }
                break;
            default:
                ubyte unknown_flag;
                break;
        }
    }
} RUN;

// a struct to represent the variable-width integers used in RLE raster line headers
typedef struct {
    ubyte raw_value;
    local int value = raw_value;
    local int size = 1;
    if ((value >> 7) == 1) {  // if highest bit is set, indicates value is 2 bytes long
        ubyte raw_value;
        value = ((raw_value[0] & 0b01111111) << 8) | raw_value[1];  // combines both bytes and strips the flag bit
        size++;
    }
} VARINT <fgcolor=cYellow, read=Str( "%i", this.value )>;
//TODO: add VARINT write function

// LINE
typedef struct {
    local int ct_header_bytes = 0;

    VARINT line_length;
    ct_header_bytes += line_length.size;

    VARINT offset;
    ct_header_bytes += offset.size;

    VARINT ct_pixels;
    ct_header_bytes += ct_pixels.size;

    local int bytes_used = ct_header_bytes;
    while (bytes_used < line_length.value) {
        RUN run;
        bytes_used += sizeof(run);
    }
} LINE <optimize=false/*, size=SizeLINE*/>;


int SizeLINE( LINE &line ) {
    local ubyte length_1 = ReadUByte( startof(line) );
    local int total_line_length = length_1;
    if ((length_1 >> 7) == 1) {  
        local ubyte length_2 = ReadUByte( startof(line) + 1 ); // length of line in bytes, including header
        total_line_length = ((length_1 & 0b01111111) << 8) | length_2;  // combines both bytes and strips the flag bit
    }
    return total_line_length;
}

typedef struct {
    IFF_NAME chunk_name;
    BigEndian();
    CHUNK_LENGTH chunk_length; //distance from end of chunk_length to next FRAM
    LittleEndian();
    // Check FRAM type to determine appropriate unpacking procedure
    switch(HEDR.file_type) {
        case TYPE_TERRAIN:
            struct TERRAIN {
                IMM_PIXEL pixel[1024];
                char footer[chunk_length - 2048];
            } terrain;
            break;
        case TYPE_ALPHA_MASK:
            struct ALPHA_MASK {
                ubyte mask_pixel[512];
            } alpha_mask;
            break;
        default:
            local int bytes_used = 0;
            while (bytes_used < chunk_length) {
                if (chunk_length - bytes_used < 4 && ReadUByte() == 0) {
                    ubyte padding[chunk_length - bytes_used];
                    bytes_used += sizeof(padding);
                } else {
                    LINE line;
                    bytes_used += sizeof(line);
                }
            }
            break;
    }
} FRAM_CHUNK <optimize=false>;

typedef struct {
    char chunk_name[4];
    BigEndian();
    CHUNK_LENGTH chunk_length;
    LittleEndian();
    uint16 palette_size;
    IMM_PIXEL pixels[palette_size] <optimize=false>;
} PALT_CHUNK;

typedef struct {
    IFF_NAME chunk_name;  // Always FORM
    BigEndian();
    CHUNK_LENGTH length;  //length in bytes from start of HEDR to end of file
    LittleEndian();
    IFF_NAME file_type;  // Always TGAR
    if( Strcmp(chunk_name, "FORM") != 0 ){
        Warning("[Error] Chunk name \"%s\" isn't \"FORM\". Exiting", chunk_name);
        Exit(-1);
    }
    if( Strcmp(file_type, "TGAR") != 0 ){
        Warning("[Error] File type \"%s\" isn't \"TGAR\". Exiting", file_type);
        Exit(-1);
    }
} FORM_CHUNK;

typedef struct {
    IFF_NAME chunk_name;
    BigEndian();
    CHUNK_LENGTH chunk_length;
    local int start_pos = FTell();
    LittleEndian();
    uint32 version;
    uint16 frame_count;
    ubyte bit_depth <format=decimal, fgcolor=cYellow>;
    enum <ubyte> FILE_TYPE { TYPE_OBJECT, TYPE_PORTRAIT, TYPE_TERRAIN, TYPE_ALPHA_MASK } file_type;
    uint8 mode1 <format=hex, fgcolor=cYellow>;
    uint8 mode2 <format=hex, fgcolor=cYellow>;
    uint16 offset_flag;
    
    struct SIZE {
        uint16 x;
        uint16 y;
    } size;
    
    struct HOTSPOT {
        uint16 x;
        uint16 y;
    } hotspot;
    
    struct BOUNDING_BOX {
        uint16 x_min;
        uint16 y_min;
        uint16 x_max;
        uint16 y_max;
    } bounding_box;
    
    uint8 unknown1[8]; // seems to contain max frame size
    
    uint32 offset_palette;  // number of bytes from start of file to begining of palette_size (9th byte of PALT)
    
    FRAME_SIZE frame_sizes[frame_count];
    
    uint16 anim_count;
    ANIMATION anim[anim_count];
} HEDR_CHUNK;

typedef struct {
    IFF_NAME chunk_name;
    BigEndian();
    CHUNK_LENGTH chunk_length;
    LittleEndian();
    char data[chunk_length];
} PLACEHOLDER_CHUNK;

local IFF_NAME chunk_name;
FSeek(0);
local int start_pos = 0;
while (!FEof()) {
    start_pos = FTell();
    ReadBytes(chunk_name, start_pos, 4);
    switch(chunk_name) {
        case "FORM": FORM_CHUNK FORM; FSeek(start_pos + 12); break;
        case "HEDR": HEDR_CHUNK HEDR; FSeek(start_pos + HEDR.chunk_length + 8); break;
        case "PALT": PALT_CHUNK PALT; FSeek(start_pos + PALT.chunk_length + 8); break;
        case "FRAM": FRAM_CHUNK FRAM; FSeek(start_pos + FRAM.chunk_length + 8); break;
        
        default:
            PLACEHOLDER_CHUNK chunk;
            break;
    }
}
